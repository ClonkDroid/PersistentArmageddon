# M1.2 contract-to-test map

**Gate B acceptance evidence is complete.** Acceptance tranche 1 implements recovery scenarios 01–05, tranche 2A implements scenarios 06–08, tranche 2B implements scenarios 09–10, tranche 3A implements scenarios 11–12, and tranche 3B implements scenarios 13–14. Gate C, medical benchmarks, production review, overall M1.2 completion, and M1.3 remain out of scope and are not claimed.

| Gate B scenario | Status | Dedicated evidence | Private structural evidence |
| --- | --- | --- | --- |
| 01 recovery boundary −1/exact/+1 | Implemented in tranche 1 | `gate_b_acceptance_01_recovery_boundary_minus_exact_plus_one` | `gate_b_acceptance_private_01_canonical_recovery_due_is_exact` |
| 02 independent hot/cold large-leap recovery | Implemented in tranche 1 | `gate_b_acceptance_02_independent_hot_and_cold_large_leap_recovery` | `gate_b_acceptance_private_02_hot_and_cold_recovery_work_is_exact` |
| 03 two-wound final-control gating and ordered healing | Implemented in tranche 1 | `gate_b_acceptance_03_two_wound_final_control_gates_distinct_healing` | — |
| 04 successful shock care while a separate wound bleeds | Implemented in tranche 1 | `gate_b_acceptance_04_shock_care_while_bleeding_never_starts_recovery` | — |
| 05 nonfatal recovery cancellation and absolute-deadline restart | Implemented in tranche 1 | `gate_b_acceptance_05_new_wound_cancels_and_restarts_absolute_recovery` | — |
| 06 immediate wound and endpoint-interruption matrices | Implemented in tranche 2A | `gate_b_acceptance_06_immediate_wound_consequences_and_endpoint_interruptions` | — |
| 07 authoritative hot/cold command failure atomicity | Implemented in tranche 2A | — | `gate_b_acceptance_07_world_apply_failures_are_fully_atomic` |
| 08 automatic medic/patient interruption and death precedence | Implemented in tranche 2A | `gate_b_acceptance_08_automatic_incapacity_interrupts_before_completion` | `gate_b_acceptance_08_automatic_incapacity_preserves_audit_and_cleans_indexes`; `gate_b_acceptance_08_living_death_at_completion_has_role_precedence_and_cleans_indexes` |
| 09 real-entity availability churn and exact candidate visits | Implemented in tranche 2B | — | `gate_b_acceptance_09_real_medic_churn_visits_only_eligible_candidates` |
| 10 medic/patient removal, conservation, and generation cleanup | Implemented in tranche 2B | — | `gate_b_acceptance_10_materialized_removal_cleans_both_endpoint_roles` |
| 11 elapsed hot/cold treatment and recovery cycles | Corrected after Gate B audit | — | `gate_b_acceptance_11_repeated_fidelity_cycles_preserve_medical_deadlines` (nonzero hot/cold intervals, literal fidelity/advance arrays, exact private due/hot state) |
| 12 rollback after recovery/healing/availability/interruption work | Corrected after Gate B audit | — | `gate_b_acceptance_12_late_recovery_overflow_rolls_back_all_earlier_medical_work` (literal control commit and full failed-segment rollback) |
| 13 combined active-treatment/recovery snapshot continuation and corruption rejection | Implemented in tranche 3B | — | `gate_b_acceptance_13_combined_snapshot_continuation_and_recovery_rejection` (one v7 image, literal future events, byte/digest/index parity, four exact recovery-state rejections) |
| 14 bleeding/recovery query purity including private authority | Implemented in tranche 3B | — | `gate_b_acceptance_14_bleeding_and_recovery_queries_are_fully_pure` (repeated permutations, literal projections, unchanged authority/indexes/counters) |

Historical tests in `sim-core/tests/medical.rs` remain supplemental regression coverage only; they are not substitutes for the dedicated acceptance scenarios above.

## Gate C1 — canonical snapshot v7 medical persistence

**Gate C1 acceptance evidence is complete.** Evidence tranche 2A adds a test-only schema walk and canonical-v7 medical corruption/death-cause coverage without adding a production parser or snapshot format. Tranche 2A-R9 adds the dedicated mixed-world deterministic continuation; Gate B scenario 13 remains supplemental only.

| Gate C1 cluster | Completed evidence |
| --- | --- |
| Test-only schema-aware v7 medical layout | `V7MedicalLayout::parse` walks every variable-length prefix and records named casualty, wound, and treatment fields/ranges; `gate_c1_schema_aware_raw_v7_medical_corruption_boundaries` proves the walk consumes the complete canonical image. |
| Canonical control and reconstructed indexes | `gate_c1_medical_corruption_matrix_rejects_exact_categories`, `gate_c1_completed_and_interrupted_history_round_trips_canonically`, and the two-patient/two-wound/two-treatment raw-byte control restore canonically and compare the indexes relevant to their stated controls. `gate_c1_mixed_world_continuation_is_literal_and_deterministic` adds the public mixed control and reconstructed continuation checkpoints. |
| Raw canonical-order, duplicate-ID, tag, Boolean, allocator, range, timestamp, truncation, and trailing-byte cases | `gate_c1_schema_aware_raw_v7_medical_corruption_boundaries` mutates named fields/ranges rather than clone-and-resnapshot substitutes and pins exact snapshot categories. The earlier reachable-state clone cases remain supplemental controls. |
| Completed and interrupted audit history | `gate_c1_completed_and_interrupted_history_round_trips_canonically` creates both statuses through public commands and checks canonical bytes, digest, records, and reconstructed two-sided history. |
| Six persistent death causes with medical history | `gate_c1_all_six_death_causes_preserve_medical_history` now produces Dehydration, Starvation, ImmediateTrauma, Hemorrhage, and TraumaticShock through public commands/transitions with retained wound/treatment history and literal causes/times. Exhaustion uses exactly one schema-level control because M1.2 exposes no public Exhaustion transition. Every case is restored twice with canonical bytes, digest, IDs, ledgers, records, and rebuilt ownership/history indexes. |
| Allocator exhaustion boundary | `gate_c1_medical_corruption_matrix_rejects_exact_categories` restores canonical `u64::MAX` allocator snapshots and proves the following public allocation fails atomically; the raw suite separately rejects retained `id == next_id`. |
| Bleeding aggregation overflow domain | Production checked addition remains. The literal bound `(u32::MAX as u64) * 1_000 < u64::MAX` proves no v7 snapshot can encode enough maximum-rate wound records to overflow `u64`, so no fabricated overflow snapshot is claimed. |
| Tranche 2A boundary, relationship, ledger, and truncation additions | `gate_c1_2a_omitted_medical_boundaries_ledgers_and_status_payloads` adds named casualty blood/shock/incapacity controls, casualty/wound and shock/casualty ownership failures, all four controlled/healed combinations, medic/faction/cell/activity relationship failures, stale endpoint/owner and missing-target raw cases, medical and nonzero food/water ledger corruptions, and named allocator/count/record truncations. This is additional evidence only; the still-unimplemented matrix items are not claimed by this row. |
| Tranche 2A-R1 recovery and treatment-status payload/timing/truncation evidence | `gate_c1_2a_r1_recovery_treatment_status_timing_and_truncation` restores separate canonical public recovery, Active hemostatic, Completed hemostatic, and Interrupted controls; pins recovery option shape, deadline/interval, terminal physiology, retained-wound compatibility, treatment start/status timing, completion state, interruption reason/death-time rules, and exact present-payload/record-end truncations. `gate_c1_schema_aware_raw_v7_medical_corruption_boundaries` now preserves duration in its future-start corruption so it reaches `treatment time`. |
| Tranche 2A-R2 canonical ordering and entity/target integrity | `gate_c1_2a_r2_treatment_order_entity_references_and_wound_targets` swaps two complete, differently sized retained treatment records and pins `noncanonical treatment order`; constructs named live, free-slot, stale/reused-generation, and bounded out-of-range entity controls and pins casualty, wound, active-medic, and active-patient reference rejection; and rejects both an allocator-valid absent historical wound target and an existing wound owned by another patient as `treatment target`. Each raw mutation is paired with a byte-identical canonical public-command restore control. |
| Tranche 2A-R3 active eligibility and treatment target/kind evidence | `gate_c1_2a_r3_active_endpoint_eligibility_and_treatment_targets` publicly constructs and pins dead-medic, dead-patient, and incapacitated-medic interruption controls before recomposing only their impossible Active status; proves an already-incapacitated living patient remains a canonical Active recipient; rejects Hemostatic-without-wound, Shock-with-wound, controlled and healed Hemostatic targets, and a target created after treatment start with exact categories and byte-identical nearby controls. |
| Tranche 2A-R4 casualty and wound numeric-boundary evidence | `gate_c1_2a_r4_casualty_and_wound_numeric_boundaries` pins canonical blood, shock, remainder, incapacity, materialization, wound-component, creation-time, and fatal-authority controls; uses named v7 fields for isolated out-of-range and relationship corruptions; and proves byte-identical restore, digest, ownership, allocator, wound-membership, and bleeding-index state. |
| Tranche 2A-R5 treatment cost/duration numeric-boundary evidence | `gate_c1_2a_r5_treatment_cost_duration_numeric_boundaries` independently constructs nonzero-clock Active Hemostatic and Shock controls, pins literal records, relationships, indexes, ledgers, and canonical v7 fields, isolates cost and duration boundary−1/+1 corruptions, and proves the checked-addition `u64::MAX` neighbor and one-tick overflow for both kinds. |
| Tranche 2A-R6 active-treatment endpoint conflicts | `gate_c1_2a_r6_active_treatment_endpoint_conflicts` restores a literal nonzero-clock control containing simultaneous Active Hemostatic and Shock treatments, then isolates shared-medic, shared-patient, and both cross-role endpoint collisions and pins `active treatment conflict` without changing canonical bytes or neighboring validation categories. |
| Tranche 2A-R7 combined nonzero resource ledgers | `gate_c1_2a_r7_combined_nonzero_resource_ledgers` publicly constructs simultaneous positive food, water, and medical source/carried/consumed/lost terms, restores every authoritative record and derived index canonically, then schema-awarely isolates all twelve equation-term mismatches and all six checked-addition hazards as `resource ledger`. |
| Tranche 2A-R8 literal death ordering and independent restores | `gate_c1_all_six_death_causes_preserve_medical_history` pins the public wound/treatment setup, literal completion/death/interruption ordering and counts, complete retained authority and derived indexes, and separately named first/second canonical restores for all five publicly reachable death causes. Exhaustion remains one explicitly schema-level control because M1.2 has no public Exhaustion transition. |
| Tranche 2A-R9 dedicated mixed-world continuation | `gate_c1_mixed_world_continuation_is_literal_and_deterministic` publicly constructs the mixed lifecycle and proves it through four named, complete hand-authored checkpoint fixtures at clocks 12 (initial), 12 (post-cleanup/generation reuse), 22 (post-completion), and 60 (final). Each fixture independently owns seed/RNG and complete scheduling authority as well as soldier/spec/living/inventory authority, casualty/wound/treatment records and allocators, reconstructed due/ownership/history/availability/cell indexes, and literal conserved resources. Every source and fresh restore passes its complete fixture before byte/digest parity. The source and every cleanup- or completion-checkpoint resume independently pass the same literal clock-22/clock-60 outcomes and cumulative history covering indexed selection, both interruption modes, death, removal/loss, generation reuse, completion, recovery, healing, ration consumption, and deterministic future allocation. |

Gate C1 is accepted on the complete evidence above. Gate B scenario 13 remains supplemental and does not substitute for tranche 2A-R9.

Protocol Gate C2, benchmark expansion/full manual workload, production review, readiness, overall M1.2 completion, M1.3, merge, and species/faction physiology, doctrine, morale, routing, Painboyz, mob cohesion, and Waaagh behavior remain explicitly pending.

## Gate C2 — strict M1.2 server protocol

**Gate C2 remains pending.** The server bridge preserves duplicate JSON keys until they can be rejected, requires exact command-specific key sets and scalar types, and uses explicit stable enum mappings. F3-F4 gives each legacy row complete row-owned initial/final authority and applies the same final fixture before restore parity; it also runs the busy/no-eligible consequence only from a source-and-restore-verified complete pre-state. Together with F3-F3, the successful medical fixtures remain independently literal. F4 completes the exhaustive real-HTTP malformed command-shape/type/range cross-product. F5 completes wound-domain and patient-identity semantics. F6 adds the exact 16-row direct/request treatment-target matrix for Hemostatic wound identity/status and Shock applicability only; the remaining medic/endpoint, requested-medic, and interruption-ID semantic tranches remain pending. This gate changes only `sim-server` protocol tests and acceptance evidence; casualty scheduling, resource rules, snapshots, core constants, and production `sim-core` behavior are unchanged.

| Gate C2 cluster | Concrete evidence |
| --- | --- |
| Legacy compatibility and wrong-type rejection | `gate_c2_legacy_real_http_compatibility_and_wrong_typed_extras` gives each null-heavy success row complete row-owned initial and final fixtures covering absence, stockpile, resource, clock, and digest authority; the exact same final fixture validates source and restore before byte/digest parity. Its accepted Boolean, string/enum, u16, u32, and u64/ID wrong-typed-extra rows retain byte/digest atomicity. |
| Exact medical command shapes and literal core commands | `gate_c2_exact_medical_command_shapes_parse_to_literal_commands` hand-authors every Hemostatic/Shock direct/request shape plus wound and interruption commands; `gate_c2_strict_parser_rejects_malformed_shapes_ranges_and_duplicates` pins missing, null, duplicate, type, range, kind, combination, unknown-field, and trailing-input rejection. |
| Real HTTP wound, direct treatment, interruption, and late-boundary cleanup | `gate_c2_f3_wound_hemostatic_interruption_and_late_advance` uses independently literal setup, post-wound, Active, Interrupted, and clock-20 fixtures on source and restore; second-wound, busy/no-eligible, released-endpoint treatment-allocation, malformed unchanged-restore, and no-late-completion probes each apply consequences only to fixture-verified restored authority and pin allocator and lifecycle consequences. |
| Real HTTP requested Shock and deterministic selection | `gate_c2_f3_requested_shock_selects_lowest_eligible_medic` uses independently literal setup/post-wound/Active fixtures, omits `wound`, proves the losing medic services a separately wounded patient with `TreatmentId(1)`, and applies complete literal source/restore fixtures before and after selection and at clocks 14 and 15. |
| Real HTTP completion, recovery, and healing continuation | `gate_c2_f3_hemostatic_completion_recovery_and_healing` uses independently literal setup/post-wound/Active/clock-9/Completed/clock-15/terminal fixtures; Completed/final allocator and clock-21 no-stale-work probes start from fixture-verified restores, use independently authored command/event wound specifications, and retain complete literal post-probe authority. |
| Complete event, enum, interruption reason, death cause, and medical error naming | `gate_c2_event_enum_reason_cause_and_error_serialization_matrix` uses literal JSON for every medical/recovery event shape, both treatment kinds and wound-option shapes, all six interruption reasons, all six death causes, and every M1.2 error code while retaining `snapshot_invalid`. |
| Malformed/field-shape/numeric/range rejection and byte-plus-digest atomicity | `gate_c2_http_malformed_matrix_is_byte_and_digest_atomic` uses six independently literal command shapes and a complete seed-77/stockpile-9 authority fixture with digest `0x2bd78a3af7ee2a90`. Through real HTTP it pins exactly 34 missing/null required-field rows, 182 recognized-but-inapplicable rows (including both Shock wound forms), 78 numeric wrong-type rows, 10 overflowing ID rows, three overflowing wound-component rows, and 29 duplicate allowed-key rows, plus version/command/kind, unknown-field, non-object, malformed, truncated, and trailing-input controls. Every uniquely named row starts from a fresh source, verifies source and first restore completely, requires exact HTTP 400/body, then proves unchanged complete authority, canonical bytes, literal digest, and a second restore. The complete semantic-rejection and accepted-domain-boundary matrix remains explicitly pending. |
| Wound-domain and patient-identity semantic boundaries | `gate_c2_f5_literal_rows_execute_and_count_authority` executes two literal row tables containing exactly eight rejection rows and three accepted component-1000 rows. Those executed rows are also the 8/3/11 counting and global-name-uniqueness authority. Each row contains its complete hand-authored request and outcome envelope and selects only parameterless, context-specific source/pre/post fixtures. The fixtures independently enumerate complete nested soldier, living, wound, casualty, absence, resource, and digest authority; accepted post fixtures separately author `wounds` and `wounds_of`. Every iteration creates fresh authority, verifies the literal pre-state before and after restore, uses real HTTP, compares the entire envelope, verifies the complete literal post-state before and after restore, and—for rejection rows—pins unchanged literal clock/digest plus byte-identical snapshots. |
| F6 treatment-target semantic matrix | `gate_c2_f6_treatment_target_semantic_matrix` executes one 16-row, globally unique, execution-authoritative literal table. Both direct and requested shapes cover ordinary absent and wire-valid maximum wound IDs, another living patient's retained uncontrolled wound, controlled and terminal healed wounds with completed treatment history, a wound deleted by explicit owner removal, a living patient without casualty authority, and a bleeding-only casualty with literal zero shock. Every row uses real HTTP, exact complete rejection envelopes, a fresh eligible same-cell medic context, canonical pre/post restore checks, and byte-plus-literal-digest rejection atomicity. |
| F7-A named-medic identity, role, and death semantics (partial F7) | `gate_c2_f7a_named_medic_identity_role_death_semantic_matrix` executes one five-row literal table covering ordinary-absent, stale-generation, wire-valid maximum, non-Medic-role, and retained-dead named medics through direct `start_treatment`, with fresh public contexts, complete literal HTTP-200 rejection envelopes, fixture-first double restore, and byte-plus-literal-digest atomicity. F7-B faction/cell/activity/incapacitation, F7-C inventory/self-treatment, F7-D endpoint occupancy, direct-patient/requested-treatment semantics, and interruption IDs remain pending. |
| Remaining treatment-semantic matrix | F7-B faction/cell/activity/incapacitation, F7-C inventory/self-treatment, F7-D endpoint occupancy, direct-patient and requested-treatment patient/availability semantics (including no eligible medic and ineligible/busy cohorts), and invalid/absent/wire-valid-maximum/completed/already-interrupted treatment IDs remain pending. |

Benchmark expansion, the full 2,410,000-soldier medical workload, production review, readiness, overall M1.2 completion, merge, M1.3, and species/faction physiology, doctrine, morale, routing, Painboyz, mob cohesion, and Waaagh behavior remain explicitly pending. PR #5 remains draft.
